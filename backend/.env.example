# ============================================================
# Chat Application Configuration
# Copy this file to .env and fill in your values
# ============================================================

# ============================================================
# Database (SQLite — zero-install, stored at data/app.db)
# No configuration needed. Just works.
# ============================================================

# ============================================================
# JWT Authentication
# Generate a secret: python -c "import secrets; print(secrets.token_hex(32))"
# ============================================================
JWT_SECRET_KEY=change-me-to-a-random-secret-key
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ============================================================
# Encryption Key (for storing API keys securely)
# Generate: python -c "import secrets; print(secrets.token_hex(16))"
# ============================================================
ENCRYPTION_KEY=change-me-to-a-random-32-char-key

# ============================================================
# LLM Providers (configure the ones you use)
# ============================================================

# OpenAI (optional)
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic API (optional - per-token billing)
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# Claude Code - uses your Claude Pro/Max SUBSCRIPTION (no per-token cost)
# Setup: npm i -g @anthropic-ai/claude-code && claude auth login
# No config needed — CLI handles auth automatically

# LM Studio (local, no API key needed)
LMSTUDIO_BASE_URL=http://localhost:1234/v1

# Ollama (local, no API key needed)
OLLAMA_BASE_URL=http://localhost:11434

# ============================================================
# Brave Search API (optional — free tier: 1 req/sec, 2000/mo)
# Get key at: https://api-dashboard.search.brave.com/app/keys
# ============================================================
# BRAVE_SEARCH_API_KEY=BSA...

# ============================================================
# Neo4j Knowledge Graph (optional)
# ============================================================
# NEO4J_URI=neo4j+s://xxxxx.databases.neo4j.io
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=your-password
# NEO4J_DATABASE=neo4j

# ============================================================
# Vector DB Paths (optional - defaults to backend/data/)
# ============================================================
# CHROMA_MESSAGES_PATH=backend/data/chroma_messages
# CHROMA_MEMORIES_PATH=backend/data/chroma_memories
# CHROMA_NEGATIVE_PATH=backend/data/chroma_negative

# ============================================================
# Timezone
# ============================================================
# IANA timezone name. Used to tell the LLM the current time and
# schedule notifications correctly. Auto-detected from OS if empty.
# Examples: America/Los_Angeles, America/New_York, Europe/London, Asia/Tokyo
TIMEZONE=America/Los_Angeles

# ============================================================
# Server
# ============================================================
HOST=0.0.0.0
PORT=8000
DEBUG=false
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================
# HTTPS / SSL (Optional — leave empty for plain HTTP)
# ============================================================
# Generate a self-signed cert for LAN use:
#   openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 365 -nodes
# Then set both paths:
# SSL_CERTFILE=cert.pem
# SSL_KEYFILE=key.pem

# ============================================================
# Network Security
# ============================================================
# The app is LAN/VPN-only by default. All requests from public IPs are blocked.
# Standard private ranges are always allowed: 10/8, 172.16/12, 192.168/16,
# 127/8, 100.64/10 (Tailscale/ZeroTier).
#
# Add extra CIDR ranges if your VPN uses non-standard subnets:
# ALLOWED_NETWORKS=10.8.0.0/24,fd00::/8
ALLOWED_NETWORKS=
